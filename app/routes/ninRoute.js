const express = require('express');
const router = express.Router();


// // const {check_admin, check_authToken} = require ('../middlewares/check-auth');
// const ninController = require('../controllers/ninController');

// //route to add phone to NIN
// router.post('/add/phone', ninController.add_new_phone_to_nin);
// //route to verify the NIN Phone
// router.post('/verify/phone', ninController.verify_phone_link);
// module.exports = router;